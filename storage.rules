rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Anyone can read/download PDFs
    match /pdfs/{year}/{allPaths=**} {
      allow read: if true;
      // Only authenticated users (admins) can upload or delete files
      allow write: if request.auth != null;
    }
  }
}
